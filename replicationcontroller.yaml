apiversion: v1
kind: ReplicationController
metadata:
  name: my-ReplicationController
spec: 
  minReadySeconds: 3
  replicas: 2
  template:
    metadata:
      spec:
        containers:
          - name: rcexample
            image: nginx:1.16
            ports:
              - containerport: 80

